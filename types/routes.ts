// Removed incorrect module augmentation that narrowed router types and
// caused compile errors across the app. Expo Router already ships proper
// typings when `experiments.typedRoutes` is enabled in app.json.
// Keeping this file as a placeholder to avoid accidental re-introduction.

export { };

